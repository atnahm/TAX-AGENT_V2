import easyocr
import base64

reader = easyocr.Reader(['en'])
with open("tax-frontend/placeholder.png", "wb") as f: # Let's create an empty image or use an existing one if possible
    # We can't do this easily. Let's just create an array.
    pass
